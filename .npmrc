# onnxruntime-node's postinstall downloads the CUDA 12 execution provider from a
# NuGet feed on linux/x64 (hundreds of MB, and a hard failure when offline). Kun
# runs Kokoro on the CPU provider, which ships inside the package for every
# platform, so the extra download is skipped everywhere.
onnxruntime-node-install=skip
