# 校招保温工作台 埋点事件清单 Event List

| 事件名称 eventCode | 采集方式 collection | 上报时机 trigger | 设备标识 A2 | 用户标识 skill_user | Skill名称 skill_name | 运行平台 skill_platform | 操作系统 skill_os | Skill版本 skill_version | 私有参数 params |
|---------|:------------:|----------|-------|-------|-------|-------|-------|-------|-------|
| `skill_invoked` Skill调用 | Hook&track | 用户加载skill时 | 机器指纹 | whoami自动采集 | warming-recruit-manager | 运行时自动检测 | 运行时自动检测 | 从配置自动读取 | `skill_name` Skill名称；`invoke_source` 调用来源（command/keyword/automation）；`user_login_name` 招聘经理loginName；`session_id` 会话ID；`intent_category` 意图分类（可选，query/script/reminder/push/automation/notify） |
| `task_completed` 任务完成 | Hook&track | 场景主任务完成时 | 机器指纹 | whoami自动采集 | warming-recruit-manager | 运行时自动检测 | 运行时自动检测 | 从配置自动读取 | `skill_name` Skill名称；`scene` 场景标识（A_query/B_script/C_reminder/D_wechat/E_automation/F_hrclaw）；`duration_ms` 耗时毫秒；`session_id` 会话ID；`fail_reason` 失败归因（可选，skill_bug/llm_limitation/user_cancel/dependency_error/timeout） |
| `error_occurred` 异常发生 | track | 任何异常路径发生时 | 机器指纹 | whoami自动采集 | warming-recruit-manager | 运行时自动检测 | 运行时自动检测 | 从配置自动读取 | `skill_name` Skill名称；`error_scene` 异常场景（data_source/identity/query/hrclaw_send/wechat_send/automation/org_resolve/other）；`session_id` 会话ID；`error_code` 错误码（可选）；`error_message` 脱敏错误摘要（可选） |
| `session_end` 会话结束 | Hook | 会话结束或skill退出时 | 机器指纹 | whoami自动采集 | warming-recruit-manager | 运行时自动检测 | 运行时自动检测 | 从配置自动读取 | `skill_name` Skill名称；`session_id` 会话ID；`scenes_used` 使用场景列表逗号分隔；`total_duration_ms` 会话总耗时毫秒；`total_events_fired` 触发事件总数 |
| `scene_a_query` 场景A数据查询 | track | 场景A查询完成并输出结果后 | 机器指纹 | whoami自动采集 | warming-recruit-manager | 运行时自动检测 | 运行时自动检测 | 从配置自动读取 | `query_scope` 查询范围（my_candidates/by_tutor/by_leader/by_org/my_plus_org）；`is_full_org_view` 是否全组织视角（true/false）；`org_filter_type` 组织过滤字段名（可选，bg_name_cn/dept_name_cn/center_name_cn/org_full_name_cn/na）；`result_total` 查询结果总人数；`result_signed` 已签人数；`result_broken` 毁约人数；`risk_high_count` 高风险人数；`risk_medium_count` 中风险人数；`no_mentor_count` 导师未填写人数；`no_leader_count` 直接上级未填写人数；`pre_entry_30d_count` 30天内预计入职人数；`link_requested` 是否请求链接（yes/no）；`zhaopin_mcp_used` 是否调用补充数据源（yes/no/unavailable）；`deep_analysis_used` 是否启用V4深度分析（yes/no）；`focus_p1_p2_count` V4重点关注人数；`stable_candidate_count` V4稳定签约识别人数；`user_login_name` 招聘经理loginName；`session_id` 会话ID |
| `scene_b_script` 场景B话术生成 | track | 场景B话术生成并输出后 | 机器指纹 | whoami自动采集 | warming-recruit-manager | 运行时自动检测 | 运行时自动检测 | 从配置自动读取 | `template_id` 话术模板（S1_welcome/S2_first_touch/S3_welcome_pack/S4_routine/S5_pre_entry/S6_reengage/S7_risk_check）；`candidate_risk_level` 候选人风险等级（low/medium/high/lost）；`has_resume_hook` 是否注入简历Hook（yes/no）；`has_interview_hook` 是否注入面评Hook（yes/no）；`multi_version` 是否多版本（yes/no）；`zhaopin_mcp_used` 是否调用补充数据源（yes/no/unavailable）；`deep_analysis_used` 是否启用V4深度分析（yes/no）；`attention_priority` V4关注优先级（可选，P1/P2/P3/P4）；`stability_level` V4稳定签约等级（可选，高/中/低）；`user_login_name` 招聘经理loginName；`session_id` 会话ID |
| `scene_c_reminder` 场景C保温提醒 | track | 场景C播报生成完成时 | 机器指纹 | whoami自动采集 | warming-recruit-manager | 运行时自动检测 | 运行时自动检测 | 从配置自动读取 | `trigger_type` 触发方式（auto/explicit/skipped）；`l1_count` 红灯紧急事项数；`l2_count` 黄灯重要事项数；`l3_count` 绿灯常规事项数；`no_mentor_in_l1` L1中导师未填写条目数；`pre_entry_in_l1` L1中临近入职条目数；`user_login_name` 招聘经理loginName；`session_id` 会话ID |
| `scene_d_wechat` 场景D企微推送 | track | 企微群推送完成时 | 机器指纹 | whoami自动采集 | warming-recruit-manager | 运行时自动检测 | 运行时自动检测 | 从配置自动读取 | `action` 动作（push_instant/schedule_created/schedule_updated）；`push_result` 推送结果（success/failed/mcp_unavailable）；`frequency` 频率（daily/weekly/custom/instant_only）；`query_scope` 推送覆盖查询范围；`user_login_name` 招聘经理loginName；`session_id` 会话ID |
| `scene_e_automation` 场景E自动化任务 | track | 自动化任务创建/更新/删除完成时 | 机器指纹 | whoami自动采集 | warming-recruit-manager | 运行时自动检测 | 运行时自动检测 | 从配置自动读取 | `action` 动作（created/updated/deleted）；`frequency_type` 频率类型（daily/weekly/custom）；`query_scope` 自动化覆盖查询范围；`with_wechat_push` 是否叠加企微推送（yes/no）；`user_login_name` 招聘经理loginName；`session_id` 会话ID |
| `scene_f_hrclaw` 场景F HRClaw通知 | track | HRClaw邮件/企微Tips发送完成后 | 机器指纹 | whoami自动采集 | warming-recruit-manager | 运行时自动检测 | 运行时自动检测 | 从配置自动读取 | `channel` 通知渠道（mail/workchat_tips）；`notify_target` 通知对象（tutor/leader/both）；`template_type` 模板类型（single/multi）；`oa_login_result` OA登录结果（auto_login/manual_login/failed）；`send_result` 发送结果（success/failed）；`receiver_count` 收件人数量；`candidate_count` 涉及候选人数量；`has_resume_link` 是否包含简历链接（yes/no/partial）；`has_employee_subtype` 是否包含员工子类型（yes/no）；`use_browser_automation` 是否使用浏览器自动化（yes/no）；`fallback_reason` 回退原因（可选，playwright_not_installed/oa_login_failed/user_refused/na）；`user_login_name` 招聘经理loginName；`session_id` 会话ID |
