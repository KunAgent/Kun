# 社招指标变更决策板

> 159 处变更归并为以下「变更模式」，请按类拍板

> 生成时间：2026-06-09

## 总览

| 类别 | 模式数 | 涉及变更数 |
| --- | --- | --- |
| 已知勘误（自动跳过） | 3 | 51 |
| **待业务决策** | 3 | 85 |
| 灰色地带（建议你看一眼） | 3 | 53 |

## 自动跳过的已知勘误

### FORMAT_groupby 字段前缀化简
- 模式：group by 字段名前缀化简
- 影响 31 处变更，涉及 31 个指标
- 处理方式：✅ README 已记录勘误，跳过

### KNOWN_FIX_是否标志位
- 模式：is_xxx = 1 → is_xxx = 是
- 影响 19 处变更，涉及 19 个指标
- 处理方式：✅ README 已记录勘误，跳过

### KNOWN_FIX_是否标志位（取值逻辑文案）
- 模式：is_xxx = 1 → is_xxx = 是
- 影响 1 处变更，涉及 1 个指标
- 处理方式：✅ README 已记录勘误，跳过

## ⚠️ 待业务决策的真变更

### NEW_FIXED_FILTER_国家筛选加固定

- **模式**：新增固定 location_country LIKE %中国%
- **影响**：23 处变更，涉及 23 个指标

**涉及指标列表**：
- `发起部门内专业面试未提交数`（字段：固定查询条件）
- `发起通道面委面试未提交数`（字段：固定查询条件）
- `HR薪资谈判通过率`（字段：固定查询条件）
- `发起部门内专业面试数`（字段：固定查询条件）
- `发起用人决策者面试未提交数`（字段：固定查询条件）
- ... 还有 18 个

**示例对比**（指标：`发起部门内专业面试未提交数`，字段：`固定查询条件`）：
```
旧: 【查询规则】
所有条件为 AND 关系，同时生效

【筛选条件】
1、员工类型ID（Report_Recruit_Flow_Detail.staff_type_id）= 2
2、流程ID（Report_Recruit_Flow_Detail.flow_id） = 3

新: 【查询规则】
所有条件为 AND 关系，同时生效

【筛选条件】
1、员工类型ID（Report_Recruit_Flow_Detail.staff_type_id）= 2
2、流程ID（Report_Recruit_Flow_Detail.flow_id） = 3
3、3、岗位所属国家（Report_Recruit_Flow_Detail.location_country_name） like '%中国%'
```

❓ **请拍板**：这个变化是要采纳吗？

### REAL_CHANGE_groupby 字段集变化

- **模式**：+['recruit_post_org_full_name'] / -[]
- **影响**：31 处变更，涉及 31 个指标

**涉及指标列表**：
- `发起部门内专业面试未提交数`（字段：组织维度）
  - 变更：+['recruit_post_org_full_name'] / -[]
- `发起通道面委面试未提交数`（字段：组织维度）
  - 变更：+['recruit_post_org_full_name'] / -[]
- `发送offer数`（字段：组织维度）
  - 变更：+['recruit_post_org_full_name'] / -[]
- `薪资谈判通过数`（字段：组织维度）
  - 变更：+['recruit_post_org_full_name'] / -[]
- `HR薪资谈判通过率`（字段：组织维度）
  - 变更：+['recruit_post_org_full_name'] / -[]
- ... 还有 26 个

**示例对比**（指标：`发起部门内专业面试未提交数`，字段：`组织维度`）：
```
旧: group by id
（通过 unnest(split(cols, '.')) 展开组织路径后，按 id 聚合）

新: group by recruit_post_org_full_name
```

❓ **请拍板**：这个变化是要采纳吗？

### REAL_CHANGE_groupby 字段集变化

- **模式**：+[] / -['is_secret_post', 'recruit_post_org_id_cb']
- **影响**：31 处变更，涉及 31 个指标

**涉及指标列表**：
- `发起部门内专业面试未提交数`（字段：岗位维度）
  - 变更：+[] / -['is_secret_post', 'recruit_post_org_id_cb']
- `发起通道面委面试未提交数`（字段：岗位维度）
  - 变更：+[] / -['is_secret_post', 'recruit_post_org_id_cb']
- `发送offer数`（字段：岗位维度）
  - 变更：+[] / -['is_secret_post', 'recruit_post_org_id_cb']
- `薪资谈判通过数`（字段：岗位维度）
  - 变更：+[] / -['is_secret_post', 'recruit_post_org_id_cb']
- `HR薪资谈判通过率`（字段：岗位维度）
  - 变更：+[] / -['is_secret_post', 'recruit_post_org_id_cb']
- ... 还有 26 个

**示例对比**（指标：`发起部门内专业面试未提交数`，字段：`岗位维度`）：
```
旧: group by post_id, post_name_cn, is_secret_post, recruit_post_org_id_cb

新: group by post_id, post_name_cn
```

❓ **请拍板**：这个变化是要采纳吗？

## 🔍 灰色地带（建议你看一眼）

### REWRITE_动态查询条件_描述重组

- **模式**：描述顺序/默认值文案重组
- **影响**：40 处变更，涉及 40 个指标

**示例**（指标：`发起部门内专业面试未提交数`，字段：`动态查询条件`）：
```
旧前200字: 【查询规则】
- 用户未提供任何条件时，使用所有条件的默认值进行查询
- 用户提供了部分条件时，已提供条件使用用户值，未提供条件继续使用默认值
- 所有条件为 AND 关系，同时生效

【筛选条件】
1、岗位管理主体（manager_unit_name_cn）：非必选，默认=腾讯集团本部，可更换，支持跨维度聚合
2、岗位专业职位（mapping_position_name）：非必选，默认=全部，可

新前200字: 【查询规则】
- 用户未提供任何条件时，使用所有条件的默认值进行查询
- 用户提供了部分条件时，已提供条件使用用户值，未提供条件继续使用默认值
- 所有条件为 AND 关系，同时生效

【筛选条件】
1、岗位管理主体（manager_unit_name_cn）：非必选，默认=腾讯集团本部，可更换，支持跨维度聚合
2、岗位专业职位（mapping_position_name）：非必选，默认=全部，可
```

### UNCATEGORIZED

- **模式**：动态查询条件 内容差异
- **影响**：1 处变更，涉及 1 个指标

**示例**（指标：`社招在招需求数`，字段：`动态查询条件`）：
```
旧前200字: 【查询规则】
- 用户未提供任何条件时，使用所有条件的默认值进行查询
- 用户提供了部分条件时，已提供条件使用用户值，未提供条件继续使用默认值
- 所有条件为 AND 关系，同时生效

【筛选条件】
1、岗位管理主体（manager_unit_name_cn）：非必选，默认=腾讯集团本部，可更换，支持跨维度聚合
2、岗位专业职位（mapping_position_name）：非必选，默认=全部，可

新前200字: 【查询规则】
- 用户未提供任何条件时，使用所有条件的默认值进行查询
- 用户提供了部分条件时，已提供条件使用用户值，未提供条件继续使用默认值
- 所有条件为 AND 关系，同时生效

【筛选条件】
1、岗位管理主体（manager_unit_name_cn）：非必选，默认=腾讯集团本部，可更换，支持跨维度聚合
2、岗位专业职位（mapping_position_name）：非必选，默认=全部，可
```

### UNCATEGORIZED

- **模式**：取值逻辑 内容差异
- **影响**：12 处变更，涉及 12 个指标

**示例**（指标：`面试中`，字段：`取值逻辑`）：
```
旧前200字: 面试中人数=社招面试中人数+活水面试中人数

1. 社招面试中人数：对flow_main_id去重计数
筛选条件：且的关系
① start_intv_time＜= end_date（默认是昨天+1天，如果用户有指定日期，则替换为指定日期+1天）
② flow_end_time＞ end_date（默认是昨天+1天，如果用户有指定日期，则替换为指定日期+1天） 或 flow_end_time 为空


新前200字: 面试中人数=社招面试中人数+活水面试中人数

1. 社招面试中人数：对flow_main_id去重计数
筛选条件：且的关系
① start_intv_time＜ end_date（默认是昨天+1天，如果用户有指定日期，则替换为指定日期+1天）
② flow_end_time＞= end_date（默认是昨天+1天，如果用户有指定日期，则替换为指定日期+1天） 或 flow_end_time 为空

```

## ❌ 未自动识别的变更（需人工查看）

- 取值逻辑 内容差异：12 处
- 动态查询条件 内容差异：1 处